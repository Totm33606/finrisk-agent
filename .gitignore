# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.coverage
htmlcov/

# Input dataset (regenerate via `make dataset`)
data/*.parquet

# MLflow store: models, preprocessors, held-out splits, metrics, plots and the
# registry all live here — the pipeline writes nowhere else.
# Regenerate via `make train eval`.
mlruns/

# Node / frontend
frontend/node_modules/
frontend/dist/

# Env
.env
.env.local

# OS / editor
.DS_Store
.idea/
.vscode/
